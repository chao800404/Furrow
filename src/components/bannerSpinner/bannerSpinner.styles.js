/** @format */

import styled from "styled-components";

export const BannerSpinnerContainer = styled.div`
  width: 100%;
  height: 100vh;
  background-color: $(({theme}) =>theme.backgroundColor);
`;
